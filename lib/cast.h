#include <stdio.h>
#include <stdlib.h>
#include <inttypes.h>
#include "lexer.h"



char** get_cast(FILE* input) {
  uint64_t mask = 0;
  uint64_t mark_runner = 0;
  char** cast = (char**)malloc(64 * sizeof(char*));

  
}
